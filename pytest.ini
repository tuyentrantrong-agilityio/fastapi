[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers --cov-config=.coveragerc
markers =
    unit: mark test as a unit test (mocked, fast)
    integration: mark test as an integration test (real DB, slower)
